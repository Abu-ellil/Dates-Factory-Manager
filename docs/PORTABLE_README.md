# 🌴 Date Factory Manager - النسخة المحمولة

## مرحباً بك في مدير مصنع التمور!

هذه نسخة محمولة من البرنامج تعمل على أي جهاز Windows بدون الحاجة لتثبيت Python أو أي مكتبات خارجية.

---

## 🚀 كيفية التشغيل

### الطريقة الأسهل:
1. **اضغط كليكين** على ملف `START_SERVER.bat`
2. انتظر قليلاً حتى يفتح المتصفح تلقائياً
3. ابدأ العمل! 🎉

### الإيقاف:
- اضغط كليكين على ملف `STOP_SERVER.bat`
- أو أغلق نافذة الـ CMD السوداء

---

## 📱 الدخول من أجهزة أخرى

### من الموبايل أو جهاز آخر في نفس الشبكة:

1. **اعرف IP الجهاز:**
   - افتح CMD (اكتب `cmd` في البحث)
   - اكتب: `ipconfig`
   - ابحث عن `IPv4 Address` (مثلاً: `192.168.1.100`)

2. **افتح المتصفح من الموبايل:**
   - اكتب: `http://192.168.1.100:5000`
   - (استبدل `192.168.1.100` بـ IP جهازك)

---

## 📂 محتويات المجلد

```
DateFactoryPortable/
├── app.exe                  # البرنامج الرئيسي
├── START_SERVER.bat         # ملف التشغيل (اضغط عليه كليكين)
├── STOP_SERVER.bat          # ملف الإيقاف
├── date_factory.db          # قاعدة البيانات
├── templates/               # صفحات الويب
├── static/                  # الملفات الثابتة (CSS, JS, صور)
├── exports/                 # مجلد التصدير
└── _internal/               # ملفات النظام (لا تحذفها!)
```

---

## 💾 النسخ الاحتياطي

### نسخ احتياطي يدوي:
1. أوقف السيرفر
2. انسخ ملف `date_factory.db` إلى مكان آخر
3. شغل السيرفر مرة أخرى

### نسخ احتياطي تلقائي:
- البرنامج يعمل نسخ احتياطي تلقائي كل يوم الساعة 2 صباحاً
- الملفات تُحفظ في مجلد `exports`
- يمكنك رفعها على Telegram Bot (راجع `TELEGRAM_SETUP.md`)

---

## 🔧 حل المشاكل

### المتصفح لا يفتح تلقائياً؟
- افتح المتصفح يدوياً واكتب: `http://localhost:5000`

### رسالة خطأ "Port already in use"؟
- السيرفر يعمل بالفعل! افتح المتصفح على `http://localhost:5000`
- أو أوقف السيرفر القديم بـ `STOP_SERVER.bat` ثم شغله مرة أخرى

### لا يعمل من الموبايل؟
- تأكد أن الجهازين على نفس الشبكة (نفس الواي فاي)
- تأكد من IP الجهاز صحيح
- جرب إيقاف Firewall مؤقتاً

### البرنامج بطيء في أول مرة؟
- طبيعي! أول تشغيل يأخذ وقت أطول (10-20 ثانية)
- بعد كده هيكون أسرع

---

## 📦 نقل البرنامج لجهاز آخر

1. انسخ **المجلد كله** `DateFactoryPortable`
2. الصقه في أي مكان على الجهاز الجديد
3. اضغط كليكين على `START_SERVER.bat`
4. خلاص! 🎉

**ملاحظة مهمة:** لازم تنسخ المجلد كله، مش ملف `app.exe` لوحده!

---

## 🆘 الدعم

إذا واجهت أي مشكلة:
1. تأكد إن المجلد كامل (مافيش ملفات محذوفة)
2. جرب إعادة تشغيل الجهاز
3. تأكد إن Windows Defender مش بيمنع البرنامج

---

## 📊 المميزات

✅ لا يحتاج تثبيت Python  
✅ لا يحتاج تثبيت مكتبات  
✅ يعمل على أي Windows (7, 8, 10, 11)  
✅ يمكن نقله لأي جهاز  
✅ نسخ احتياطي تلقائي  
✅ دعم اللغة العربية كامل  

---

**صنع بـ ❤️ لمصنع التمور**
